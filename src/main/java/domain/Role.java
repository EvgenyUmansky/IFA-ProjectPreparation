package domain;

/**
 * Represents the types of users that there are in the system
 */
public enum Role {
    SYSTEM_ADMIN,
    ASSOCIATION_AGENT,
    TEAM_PLAYER,
    COACH,
    TEAM_MANAGER,
    TEAM_OWNER,
    REFEREE,
    FAN
}
